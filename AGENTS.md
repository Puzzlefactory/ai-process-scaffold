# Agent Entry Point

This file is a thin compatibility wrapper for Codex, Claude, and other coding agents.

Read `.ai/instructions.md` first and follow the workflow there.

Do not report `.ai` files as missing unless a filesystem read or list command confirms they are missing.

Keep durable project context in `.ai/`, not in chat-only memory.
